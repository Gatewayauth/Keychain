nodeLinker: node-modules

# Supply-chain age gate: refuse to resolve package versions published more
# recently than this, blunting compromised just-published releases. Matches
# renovate.json minimumReleaseAge. (Requires Yarn >= 4.10.)
npmMinimalAgeGate: "7d"
