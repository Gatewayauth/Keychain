nodeLinker: node-modules

# Supply-chain age gate: refuse to install package versions published less than
# this many minutes ago (4320 = 3 days), blunting compromised just-published
# releases. Matches renovate.json minimumReleaseAge.
npmMinimalAgeGate: 4320
